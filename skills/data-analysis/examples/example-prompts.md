# Example prompts

- Analyze `sales.csv` and tell me the highest-value regions.
- Convert `orders.xlsx` to Parquet, clean the schema, and summarize nulls.
- Inspect `events.parquet` and tell me whether it should stay in Parquet or be rewritten.
- Profile `finance.xlsx` and explain whether you will use in-memory or out-of-core processing.

